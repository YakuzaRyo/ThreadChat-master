import PorCheck
import multiprocessing
import uuid

def run(test):
    pool = multiprocessing.Pool(multiprocessing.cpu_count())

if __name__ == '__main__':
    uid = uuid.uuid1()
    print(type(uid))
    """data = []
    for i in range(81,65536):
        data.append(i)

    print(data)"""
